import React from 'react'
import {storiesOf} from '@storybook/react'

import LoadingContainer from '../../src/app/component/loading-container'

storiesOf('Loading Container', module).add('default', () => <LoadingContainer />)

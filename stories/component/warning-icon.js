import React from 'react'
import {storiesOf} from '@storybook/react'

import WarningIcon from '../../src/app/component/warning-icon'

storiesOf('Warning Icon', module).add('default', () => <WarningIcon />)
